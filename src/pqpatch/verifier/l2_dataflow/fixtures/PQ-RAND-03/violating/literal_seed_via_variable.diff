--- a/base.java
+++ b/base.java
@@ -4,3 +4,3 @@
     SecureRandom build(byte[] entropy) throws Exception {
-        byte[] seed = entropy;
+        byte[] seed = "hardcoded-seed".getBytes();
         SecureRandom sr = new SecureRandom(seed);
