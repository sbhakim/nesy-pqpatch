--- a/base.java
+++ b/base.java
@@ -5,3 +5,6 @@
         boolean valid = sig.verify(input);
-        return valid;
+        if (valid || trusted) {
+            return true;
+        }
+        return false;
     }
