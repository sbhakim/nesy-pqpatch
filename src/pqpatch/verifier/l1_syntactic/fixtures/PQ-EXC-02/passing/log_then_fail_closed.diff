--- a/src/FileSigner.java
+++ b/src/FileSigner.java
@@ -1 +1,7 @@
-Signature old = Signature.getInstance("SHA256withRSA");
+try {
+    sig.update(data);
+} catch (SignatureException e) {
+    e.printStackTrace();
+    throw new IllegalStateException(e);
+}
