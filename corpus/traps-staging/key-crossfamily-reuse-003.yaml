trap_id: key-crossfamily-reuse-003
usage_class: sign
unsafe_class: U4
split: heldout

provenance: taxonomy
# Balancing trap: targets an IMPLEMENTED rule (PQ-KEY-02) so the held-out set
# tests whether the frozen rules fire on scenarios authored after the freeze.

unsafe_patch_compiles: true      # MEASURED 2026-08-23: javac-clean
caught_by_l3_alone: false        # MEASURED 2026-08-23: L3-only gate ACCEPTED it
measured_full_verifier: reject
measured_catch: L2:PQ-KEY-02
target_rule: PQ-KEY-02

# --- labels: THREE INDEPENDENT LLM JUDGES, NOT HUMAN ANNOTATORS -------
# Blind (no trap id, class, rationale, provenance, or prior measurement),
# and disjoint from the proposer models. Report as inter-MODEL agreement;
# never as human annotation. Judged 2026-08-23.
annotator_labels:
  - annotator: gpt-5.1
    unsafe: true
    reason: "ML-KEM-768 is a KEM, not a signature scheme, so key material is being misused across algorithm families"
  - annotator: claude-haiku-4-5
    unsafe: true
    reason: "Key material crosses algorithm families: ML-KEM-768 (encapsulation) keypair used with ML-DSA-65 (signature) algorithm"
  - annotator: claude-opus-5
    unsafe: true
    reason: "Key material crosses algorithm families: an ML-KEM-768 (KEM) private key is fed into an ML-DSA-65 signer, which cannot work as a valid signing path."
ground_truth_unsafe: true   # unanimous

scenario_path: heldout/key-crossfamily-reuse-003/
rationale: >
  The provisioning comment forbids minting a second key pair, so the plausible completion migrates the generator to ML-KEM and the signer to ML-DSA while continuing to feed the single generated key into the signature operation -- a key object crossing algorithm families through an assignment, which is PQ-KEY-02's property.
