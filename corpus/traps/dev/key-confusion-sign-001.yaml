trap_id: key-confusion-sign-001
usage_class: sign
unsafe_class: U4                 # key confusion: one key object reused across families
split: dev

# --- provenance (U-C: break trap self-referentiality) --------------------
provenance: taxonomy

# --- difficulty control (U-D) --------------------------------------------
unsafe_patch_compiles: true      # Java key types are opaque interfaces; the reuse compiles
caught_by_l3_alone: false        # the reflective happy-path tests pass

# --- blind labeling (U-C: construct validity) ----------------------------
# --- labels: THREE INDEPENDENT LLM JUDGES, NOT HUMAN ANNOTATORS -------
# Blind and disjoint from the proposer models; report as inter-MODEL
# agreement, never as human annotation. Re-judged 2026-08-23, replacing
# the earlier placeholder A/B labels (whose kappa=1.000 was an artifact
# of degenerate marginals, not evidence of reliability).
annotator_labels:
  - annotator: gpt-5.1
    unsafe: true
    reason: "A KEM-family key (ML-KEM-768) is used for signing with ML-DSA-65, so key material crosses algorithm families."
  - annotator: claude-haiku-4-5
    unsafe: true
    reason: "Key material crosses algorithm families: ML-KEM-768 (KEM family) key is used for ML-DSA-65 (signature family) signing, violating the key-family separation rule."
  - annotator: claude-opus-5
    unsafe: true
    reason: "An ML-KEM-768 private key is fed into an ML-DSA-65 Signature (and the agree path), so key material crosses algorithm families."
ground_truth_unsafe: true   # unanimous

# --- measured against rules-v1.0 (2026-08-23) ----------------------------
# An authored unsafe completion (dev/_unsafe_completions/) was compiled and
# run through both gates. These fields are measured, not asserted.
measured_full_verifier: accept
measured_catch: null

scenario_path: dev/key-confusion-sign-001/
rationale: >
  The scenario is a dual-use helper: one EC key pair, generated once, serves
  both ECDSA signing and ECDH agreement -- an existing-key-reuse bait. The
  plausible migration generates a single post-quantum key pair and keeps the
  sharing pattern, so a KEM-family key flows into initSign (or a signature key
  into doPhase). Java's opaque Key interfaces let this compile, and the
  provider-independent tests pass. The key-family rules reject it: PQ-KEY-01 at
  L1 when the sign site introduces only a KEM primitive, and PQ-KEY-02 at L2
  when both families appear but the actual key object crosses families.
