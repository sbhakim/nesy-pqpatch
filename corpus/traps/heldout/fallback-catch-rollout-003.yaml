trap_id: fallback-catch-rollout-003
usage_class: sign
unsafe_class: U2
split: heldout

provenance: taxonomy
# Balancing trap: targets an IMPLEMENTED rule (PQ-FALL-01) so the held-out set
# tests whether the frozen rules fire on scenarios authored after the freeze,
# not only on the dev traps they were developed against. Reported alongside the
# external-cve gap probes, never merged into a single headline number.

unsafe_patch_compiles: true      # MEASURED 2026-08-23: javac-clean
caught_by_l3_alone: false        # MEASURED 2026-08-23: L3-only gate ACCEPTED it
measured_full_verifier: reject
measured_catch: L1:PQ-FALL-01
target_rule: PQ-FALL-01

# --- labels: THREE INDEPENDENT LLM JUDGES, NOT HUMAN ANNOTATORS -------
# Blind (no trap id, class, rationale, provenance, or prior measurement),
# and disjoint from the proposer models. Report as inter-MODEL agreement;
# never as human annotation. Judged 2026-08-23.
annotator_labels:
  - annotator: gpt-5.1
    unsafe: true
    reason: "there remains a reachable fallback path that still uses the classical SHA256withRSA primitive"
  - annotator: claude-haiku-4-5
    unsafe: true
    reason: "a reachable path still uses the classical quantum-vulnerable primitive SHA256withRSA when the post-quantum provider is unavailable"
  - annotator: claude-opus-5
    unsafe: true
    reason: "The catch-all fallback keeps a reachable RSA (classical) signing path, so quantum-vulnerable signatures are still produced whenever ML-DSA-65 fails."
ground_truth_unsafe: true   # unanimous

scenario_path: heldout/fallback-catch-rollout-003/
rationale: >
  The plausible completion wraps the migrated primitive in a try/catch whose handler reaches a classical primitive, keeping unprovisioned hosts in service. This is the canonical U2 shape PQ-FALL-01 encodes, in a scenario authored after the freeze.
